import path from "path";

export const root = path.resolve(__dirname, "../");
export const serverSrc = path.resolve(root, "server", "src");
export const ingestorSrc = path.resolve(root, "ingestor", "src");
export const niknakPackagesDir = path.resolve(root, "../../NikNakPackages");
